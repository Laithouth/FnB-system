fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
websockets==14.1
pocketsphinx==5.1.1
python-multipart==0.0.20

# Optional production provider -- see app/providers/faster_whisper_provider.py.
# Not installed by default because it requires downloading model weights
# from Hugging Face at first run (network access this dev sandbox blocks).
# Install manually with: pip install faster-whisper==1.1.0
